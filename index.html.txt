<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Mining App</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root { --orange: #FF9D00; --bg: #000000; --card-bg: #151515; --text-gray: #8A8A8E; }
        body { background-color: var(--bg); color: #fff; font-family: -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; padding: 15px; padding-bottom: 100px; }
        
        .page { display: none; }
        .page.active { display: block; }

        .card { background: var(--card-bg); border-radius: 20px; padding: 20px; margin-bottom: 15px; border: 1px solid #2C2C2E; }
        .label { color: var(--text-gray); font-size: 13px; margin-bottom: 5px; }
        .value { float: right; font-weight: 600; }
        
        .progress-container { background: #2C2C2E; height: 14px; border-radius: 10px; margin-top: 12px; overflow: hidden; }
        .progress-bar { background: var(--orange); height: 100%; width: 47%; box-shadow: 0 0 15px rgba(255, 157, 0, 0.4); }

        .btn-main { background: var(--orange); color: #000; border: none; width: 100%; padding: 18px; border-radius: 16px; font-weight: bold; font-size: 18px; margin: 10px 0; }
        
        /* Upgrade Styles */
        .upgrade-header { text-align: center; margin-bottom: 25px; }
        .upgrade-header h2 { font-size: 28px; margin-bottom: 8px; }
        .upgrade-header p { color: var(--text-gray); font-size: 13px; line-height: 1.4; padding: 0 20px; }

        .upgrade-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
        .btn-action { background: var(--orange); color: #000; border: none; padding: 10px; border-radius: 10px; font-weight: bold; width: 100%; margin-top: 10px; font-size: 13px; }

        .mode-selector { display: flex; gap: 5px; background: #1c1c1e; padding: 5px; border-radius: 12px; margin: 15px 0; }
        .mode-tab { flex: 1; text-align: center; padding: 10px; border-radius: 10px; font-size: 12px; color: var(--text-gray); }
        .mode-tab.active { background: var(--orange); color: #000; }

        .server-card { background: var(--card-bg); border-radius: 15px; padding: 15px; border: 1px solid #2C2C2E; display: flex; flex-direction: column; align-items: center; text-align: center; }
        .server-card img { width: 60px; height: 60px; margin-bottom: 10px; }

        /* Navigation */
        .nav-bar { position: fixed; bottom: 0; left: 0; right: 0; background: rgba(0,0,0,0.95); display: flex; justify-content: space-around; padding: 10px 0 30px; border-top: 1px solid #2C2C2E; backdrop-filter: blur(10px); }
        .nav-item { text-align: center; color: var(--text-gray); font-size: 11px; flex: 1; background: none; border: none; cursor: pointer; }
        .nav-item.active { color: #fff; }
        .nav-icon { font-size: 22px; margin-bottom: 4px; display: block; }
    </style>
</head>
<body>

    <div id="mine" class="page active">
        <div class="card">
            <div><span class="label">$Keys Balance</span> <span class="value">139,071</span></div>
            <div style="margin-top: 15px;"><span class="label">Energy</span> <span class="value">4000/8500</span></div>
            <div class="progress-container"><div class="progress-bar"></div></div>
        </div>
        <button class="btn-main" onclick="tg.HapticFeedback.impactOccurred('medium')">Start Mining</button>
        <h3 style="margin: 25px 0 15px 5px;">Last Blocks</h3>
        <div class="card" style="padding: 10px 20px;">
            <div style="padding: 8px 0; border-bottom: 1px solid #222;"><span class="label">Block #1831239</span><span class="value">03:15</span></div>
            <div style="padding: 8px 0;"><span class="label">Block #1831238</span><span class="value">03:14</span></div>
        </div>
    </div>

    <div id="upgrade" class="page">
        <div class="upgrade-header">
            <h2>Upgrade</h2>
            <p>Complete Tasks and earn rewards, invite friends for each active friend you will earn a bonus</p>
        </div>
        
        <div class="upgrade-grid">
            <div class="card" style="margin:0; padding: 15px;">
                <div class="label" style="text-align:center">Max Energy</div>
                <div style="text-align:center; font-size: 11px;">Lvl 5 : 8500</div>
                <button class="btn-action">Upgrade +1000</button>
            </div>
            <div class="card" style="margin:0; padding: 15px;">
                <div class="label" style="text-align:center">Restore Energy</div>
                <div style="text-align:center; font-size: 11px;">4000/8500</div>
                <button class="btn-action">Restore 100%</button>
            </div>
        </div>

        <h3 style="margin: 25px 0 10px;">Mining Mode</h3>
        <div class="mode-selector">
            <div class="mode-tab">Basic</div>
            <div class="mode-tab active">Turbo</div>
            <div class="mode-tab">Super</div>
            <div class="mode-tab">Ultimate</div>
        </div>

        <h3>Background Mining</h3>
        <div class="upgrade-grid">
            <div class="server-card">
                <div style="font-size: 30px;">⚙️</div>
                <div style="font-size: 14px; font-weight: bold; margin-top:5px">Basic</div>
                <div class="label">12,000 H/S</div>
                <button class="btn-action">Rent Now</button>
            </div>
            <div class="server-card">
                <div style="font-size: 30px;">⚒️</div>
                <div style="font-size: 14px; font-weight: bold; margin-top:5px">Turbo</div>
                <div class="label">32,000 H/S</div>
                <button class="btn-action">Rent Now</button>
            </div>
        </div>
    </div>

    <nav class="nav-bar">
        <button class="nav-item active" onclick="showPage('mine', this)">
            <span class="nav-icon">🔑</span>Mine
        </button>
        <button class="nav-item" onclick="showPage('upgrade', this)">
            <span class="nav-icon">🚀</span>Upgrade
        </button>
        <button class="nav-item">
            <span class="nav-icon">👛</span>Wallet
        </button>
    </nav>

    <script>
        const tg = window.Telegram.WebApp;
        tg.expand();

        function showPage(pageId, el) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            document.querySelectorAll('.nav-item').forEach(btn => btn.classList.remove('active'));
            document.getElementById(pageId).classList.add('active');
            el.classList.add('active');
            tg.HapticFeedback.impactOccurred('light');
        }
    </script>
</body>
</html>